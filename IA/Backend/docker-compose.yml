version: "3"

services:
  app:
    container_name: iaapp
    build:
      context: .
    ports: 
      - "8000:8000"
    volumes:
      - ./app:/app
    command: sh -c "python manage.py runserver 0:8080"